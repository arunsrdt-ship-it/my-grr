/** @type {import('tailwindcss').Config} */
export const theme = {
    extend: {
        screens: {
            'xl2': '1455px', 
             'spin-slow': 'spin 10s linear infinite', // adjust 10s for speed// this creates a new breakpoint
        },
    },
};
export const plugins = [];
